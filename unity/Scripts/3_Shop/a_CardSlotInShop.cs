using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class a_CardSlotInShop : MonoBehaviour
{
    public bool isThisSold = false;
    public GameObject soldMarker;           // 판매되었다면 판매되었다는 표시를 위한 마커
    public GameObject cardFront;
    public GameObject cardPrice;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
